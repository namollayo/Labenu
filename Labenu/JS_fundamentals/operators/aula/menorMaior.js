const numA = 47
const numB = 3

console.log(`
    Primeiro Número: ${numA}\n
    Segundo Número: ${numB}`)


    console.log("O primeiro número é igual ao segundo?", numA === numB);
    console.log("O primeiro número é menor ou igual ao segundo?",numA <= numB);
    console.log("O primeiro número é maior que o segundo?", numA > numB);
    console.log("O primeiro número é menor que o segundo?", numA < numB);

// Operadores Relacionais
// Menor, Maior, Igual que 