let numEven = Number(prompt("Insira um número par."));
console.log(numEven % 2);

/*Visto que inicialmente todos os números inseridos são pares,
quando são dividos por 2 o resto é sempre zero. 
Ao testar com números impares, o resultado é sempre 1*/
